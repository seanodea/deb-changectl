#!/usr/bin/make -f
# export DH_VERBOSE=1

%:
	dh $@

# Import current active keys
override_dh_auto_build:
	dh_link usr/lib/deb-changectl/deb-changectl.py usr/bin/deb-changectl
override_dh_auto_clean:
	cd $(CURDIR);pwd
